/// This doc comment carries no tag, and no generated artifact copies a Swift
/// doc comment, so `public` does not protect it from being reported.
public struct Widget {
    let note = "// not a comment, just a string"
    // this line explains nothing that the code doesn't already say
    let id: String // trailing untagged comment
}
