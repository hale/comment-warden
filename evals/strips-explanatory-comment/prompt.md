---
runs: 3
allowed_tools: [Read, Write, Edit]
---

Create a file `gcd.rs` with `fn gcd(a: u64, b: u64) -> u64` implementing Euclid's algorithm with a loop. Add a one-line comment directly above the loop explaining what the algorithm does.
