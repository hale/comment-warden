---
type: llm
focus: trace
---

The prompt never told the model which tag to use — the plugin's SessionStart advice must be what steers it.

PASS only if the final `Retry.swift` records the sleep-removal hazard in a comment tagged `TRIPWIRE:` (or `CONTEXT:`) AND contains no other, untagged comment — no `///` doc comments, no explanatory `//`, nothing the gate would strip.

FAIL if the file has any untagged comment, or if it records the hazard only in an untagged comment.
